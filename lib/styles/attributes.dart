import 'dart:ui';

import 'package:flutter/material.dart';

class Attributes {
  static const borderRadiusAll30 = BorderRadius.all(Radius.circular(30));

  static const borderRadiusBottomLeft30 =
      BorderRadius.only(bottomLeft: Radius.circular(30));

  static const borderRadiusBottomRight30 =
      BorderRadius.only(bottomRight: Radius.circular(30));

  static final borderAll3 = Border.all(width: 3);

  static final borderAll1 = Border.all(width: 1);
}
