import 'dart:ui';

import 'package:flutter/material.dart';

class Attributes {
  static const borderRadiusAll30 = BorderRadius.all(Radius.circular(30));

  static final borderAll3 = Border.all(width: 3);
}
